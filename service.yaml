name: le-bot
ray_serve_config:
  http_options:
    request_timeout_s: 5
  applications:
    - import_path: 'main:bot'
      runtime_env:
        pip:
          - fastapi
          - 'ray[serve]'
          - mistralai
          - pandas
          - typing
          - PyJWT
        env_vars:
          JOB_ID: a3ed164c-5b8f-4df0-aa39-63a4f324fdb6
          MISTRAL_API_KEY: Iqv4AIPgqSCiTdzZJQzBnGki0LDGIWYQ
          APP_ID: "922150"
          PRIVATE_KEY: ""
          working_dir: .
canary_percent: 100 # This field should only specified if you do a manual rollout
rollout_strategy: ROLLOUT # You may specify either ROLLOUT or IN_PLACE
config:
  access:
    use_bearer_token: False